{
  "name": "access-network",
  "version": "1.0.0",
  "description": "ACCESS — invite-only, agent-to-agent off-market investment deal network (standalone).",
  "main": "server.js",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cookie-session": "^2.1.0",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "stripe": "^16.12.0"
  }
}
